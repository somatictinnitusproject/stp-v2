// Phase I will replace this stub with EmailOctopus integration. Signature is stable — callers can rely on the (userId, phase) shape.

export async function triggerPhaseCompletionEmail(userId: string, phase: number): Promise<void> {
  console.log('[phase-emails] TODO: send phase completion email', { userId, phase })
}
