export default function BottomNav() {
  return (
    <nav
      className="fixed bottom-0 left-0 right-0 h-[64px] z-nav bg-surface border-t border-border flex items-center justify-around px-4"
      style={{ paddingBottom: 'env(safe-area-inset-bottom)' }}
    >
      {/* Phase C: dashboard, tracker, framework, exercise library, more */}
    </nav>
  )
}
